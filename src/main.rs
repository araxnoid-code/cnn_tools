use cnn_tools::{draw_plot, get_test_dataset, get_train_dataset, model_train};

fn main() {
    // let dataset = get_test_dataset();
    // let loss = traint_model(dataset);
    // draw_plot("training", loss, "training.png", (750, 750));
}
